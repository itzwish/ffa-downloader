# 📱 Visual Deployment Guide

Visual step-by-step guide for deploying to Replit + Vercel.

## Phase 1: Backend Setup on Replit

### Step 1.1: Create Replit Project
```
┌─────────────────────────────────┐
│ https://replit.com              │
└─────────────────────────────────┘
         ↓
    Click "Create Replit"
         ↓
┌─────────────────────────────────┐
│ Select "Import from GitHub"     │
└─────────────────────────────────┘
         ↓
    Paste your repo URL
         ↓
┌─────────────────────────────────┐
│ Replit imports & initializes    │
│ (takes 1-2 minutes)             │
└─────────────────────────────────┘
```

### Step 1.2: Add Secrets
```
┌─────────────────────────────────┐
│ Click 🔒 Secrets icon (left)    │
└─────────────────────────────────┘
         ↓
    Add these secrets:
    • NODE_ENV = production
    • FRONTEND_URL = (leave empty for now)
    • MAX_FILE_SIZE_MB = 500
         ↓
┌─────────────────────────────────┐
│ Secrets saved                   │
└─────────────────────────────────┘
```

### Step 1.3: Install Dependencies
```
┌─────────────────────────────────┐
│ Click "Shell" tab               │
└─────────────────────────────────┘
         ↓
    Run these commands:
    
    $ cd web
    $ npm install        (1-2 min)
    $ pip install yt-dlp (30 sec)
    $ npm start
         ↓
┌─────────────────────────────────┐
│ 🚀 Server running               │
│                                 │
│ See output:                     │
│ "Server running on              │
│  http://localhost:3000"         │
└─────────────────────────────────┘
```

### Step 1.4: Get Backend URL
```
┌─────────────────────────────────┐
│ Look at top of Replit window    │
│                                 │
│ You'll see URL like:            │
│ ytdownloader-bcd123.replit.dev  │
│                                 │
│ ⭐ COPY THIS URL ⭐             │
└─────────────────────────────────┘
```

---

## Phase 2: Frontend Setup on Vercel

### Step 2.1: Create Vercel Project
```
┌─────────────────────────────────┐
│ https://vercel.com              │
└─────────────────────────────────┘
         ↓
    Click "Add New" → "Project"
         ↓
┌─────────────────────────────────┐
│ Select "Import Git Repository"  │
└─────────────────────────────────┘
         ↓
    Choose your GitHub repo
         ↓
┌─────────────────────────────────┐
│ Vercel shows import screen      │
└─────────────────────────────────┘
```

### Step 2.2: Configure Build
```
┌─────────────────────────────────┐
│ Look for "Root Directory"       │
│                                 │
│ Change from: (default)          │
│ Change to:   web/public         │
└─────────────────────────────────┘
         ↓
    Click "Deploy"
         ↓
┌─────────────────────────────────┐
│ Vercel deploying...             │
│ (takes 1-2 minutes)             │
│                                 │
│ Then shows:                     │
│ "Congratulations! Deployed"     │
└─────────────────────────────────┘
```

### Step 2.3: Get Frontend URL
```
┌─────────────────────────────────┐
│ ⭐ COPY THIS URL ⭐             │
│                                 │
│ You'll see something like:      │
│ ytdownloader.vercel.app         │
└─────────────────────────────────┘
```

### Step 2.4: Add Environment Variable
```
┌─────────────────────────────────┐
│ Click "Settings" → "Environment │
│ Variables"                      │
└─────────────────────────────────┘
         ↓
    Add:
    Name:  REACT_APP_API_URL
    Value: https://your-replit-url.replit.dev
    
    (Use the URL you copied in Step 1.4)
         ↓
┌─────────────────────────────────┐
│ Click "Save"                    │
└─────────────────────────────────┘
         ↓
    Go back to "Deployments"
         ↓
    Click latest deployment
         ↓
    Click "Redeploy"
         ↓
┌─────────────────────────────────┐
│ Wait for redeploy (30 sec)      │
└─────────────────────────────────┘
```

---

## Phase 3: Connect Everything

### Step 3.1: Update Replit CORS
```
┌─────────────────────────────────┐
│ Go back to Replit               │
└─────────────────────────────────┘
         ↓
    Click 🔒 Secrets icon
         ↓
    Update FRONTEND_URL:
    FRONTEND_URL = https://your-vercel-domain.vercel.app
    
    (Use the URL you copied in Step 2.3)
         ↓
┌─────────────────────────────────┐
│ Go to Shell tab                 │
└─────────────────────────────────┘
         ↓
    Stop current server (Ctrl+C)
         ↓
    Restart: npm start
         ↓
┌─────────────────────────────────┐
│ Backend updated & restarted     │
└─────────────────────────────────┘
```

---

## Phase 4: Testing

### Step 4.1: Test Backend
```
Open terminal and run:

$ curl https://your-replit-url.replit.dev/api/health

You should see:
{"status":"ok","timestamp":"2024-12-24T..."}

✅ Backend working!
```

### Step 4.2: Test Frontend
```
1. Open your Vercel URL in browser
   https://your-vercel-domain.vercel.app

2. Paste YouTube URL in the input field

3. Click "Fetch Info" button

4. Wait 3-5 seconds

5. See video title & thumbnail appear

✅ Frontend + Backend connected!
```

### Step 4.3: Test Full Download
```
1. Select format: MP4 (or Audio)

2. Select quality: 720p

3. Click "Start Download"

4. Watch progress bar fill up

5. When done, file downloads to your computer

✅ Everything working perfectly! 🎉
```

---

## Success Checklist

```
┌─────────────────────────────────┐
│ ✅ Backend running on Replit    │
│ ✅ Frontend deployed on Vercel  │
│ ✅ CORS configured correctly    │
│ ✅ Environment variables set    │
│ ✅ Health check passes          │
│ ✅ Fetch info works             │
│ ✅ Download completes           │
│                                 │
│ 🎉 READY FOR PRODUCTION! 🎉    │
└─────────────────────────────────┘
```

---

## Quick Reference: URLs to Save

```
🔗 Backend (Replit):
   https://your-project.replit.dev

🔗 Frontend (Vercel):
   https://your-project.vercel.app
```

---

## Troubleshooting Visual

### Problem: Blank page on Vercel
```
┌──────────────────────────┐
│ → Check browser console  │
│ → Look for red errors    │
│ → Likely: API URL wrong  │
│ → Fix: Update env var    │
│ → Redeploy on Vercel     │
└──────────────────────────┘
```

### Problem: API fails (CORS error)
```
┌──────────────────────────┐
│ → Check Replit Secrets   │
│ → FRONTEND_URL must match│
│   Vercel domain exactly! │
│ → Restart backend        │
│ → Try again              │
└──────────────────────────┘
```

### Problem: Download fails
```
┌──────────────────────────┐
│ → Open Replit Shell      │
│ → Run: yt-dlp --version │
│ → If error: pip install  │
│   yt-dlp                 │
│ → Try lower quality      │
│ → Check file size limit  │
└──────────────────────────┘
```

---

**Need help? Read the detailed guides in the root folder.** ✨
